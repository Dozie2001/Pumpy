# Copy to .env. hardhat.config.ts loads it automatically.
# Never commit the populated file or reuse a mainnet key.
HARDHAT_VAR_SHANNON_RPC_URL=https://dream-rpc.somnia.network
# Dedicated low-balance Shannon testnet deployer: 0x followed by 64 hex chars.
HARDHAT_VAR_DEPLOYER_PRIVATE_KEY=
